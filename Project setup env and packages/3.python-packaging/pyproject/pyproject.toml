[project]
name = "myapp"
version = "0.1.0"
description = "FastAPI production app"
authors = [
    { name = "Your Name" }
]
dependencies = [
    "fastapi",
    "uvicorn[standard]"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project.optional-dependencies]
dev = ["pytest", "black", "ruff"]       // pip install .[dev]